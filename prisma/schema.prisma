generator client {
  provider = "prisma-client-js"
  output   = "../generated/prisma"
}

datasource db {
  provider = "postgresql"
}

model Tasks {
  id    String  @id @default(uuid())
  title String
  done  Boolean @default(false)
}